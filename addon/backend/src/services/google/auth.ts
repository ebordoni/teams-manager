// Fase 5 (roadmap) — integrazione Google OAuth 2.0.
// Non ancora implementata: l'app funziona in modalità locale senza Google.
// Qui verrà gestito lo scambio del codice OAuth e la persistenza dei token
// (access/refresh) in /data, seguendo lo stesso pattern di config.ts.

export function isGoogleConfigured(): boolean {
  return false;
}
